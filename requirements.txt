# Core
rich>=15.0.0
psutil>=7.2.2
pyyaml>=6.0.3
flask>=3.1.3
pillow>=12.2.0

# Network (optional on Windows — simulation mode used if missing)
scapy>=2.7.0; platform_system != "Windows" or python_version >= "3.10"

# Threat intelligence (Linux/BPI recommended)
yara-python>=4.5.4; platform_system != "Windows"

# Antivirus integrations (optional)
pyclamd>=0.4.0

# Display backends (optional — install on device)
# luma.lcd>=2.11.0
# luma.core>=2.4.2
# spidev>=3.6; platform_system == "Linux"
# RPi.GPIO>=0.7.1; platform_system == "Linux"
# waveshare-epd>=1.0.0; platform_system == "Linux"

# Mr. CrackBot wordlist AI path (optional — heuristic mode needs no extra deps)
# transformers>=4.40.0
# torch>=2.0.0

# Dev / test
pytest>=9.0.3
pytest-cov>=7.1.0

# Credential vault (AES-256-GCM + Argon2id KDF)
cryptography>=48.0.0
argon2-cffi>=25.1.0
