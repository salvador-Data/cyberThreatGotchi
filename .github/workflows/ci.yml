name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install bandit
      - name: Generate sprite assets
        run: python assets/sprites/generate_sprites.py
      - name: Generate marketing graphics
        run: python assets/marketing/generate_graphics.py
      - name: Validate shop configs
        run: python scripts/check_shop.py
      - name: Run tests
        env:
          CTG_SIMULATION: "1"
        run: pytest tests/ -v
